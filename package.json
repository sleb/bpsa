{
  "name": "bpsa26",
  "private": true,
  "scripts": {
    "dev": "bun --cwd site dev",
    "build": "bun --cwd site run build",
    "preview": "bun --cwd site run preview",
    "studio": "bun --cwd studio dev"
  }
}
