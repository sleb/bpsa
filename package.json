{
  "name": "bpsa26",
  "private": true,
  "workspaces": [
    "site",
    "studio"
  ],
  "scripts": {
    "dev": "bun --filter @bpsa/site dev",
    "build": "bun --workspaces run build",
    "preview": "bun --filter @bpsa/site preview",
    "studio": "bun --filter @bpsa/studio dev",
    "check": "biome check --write",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15"
  }
}
